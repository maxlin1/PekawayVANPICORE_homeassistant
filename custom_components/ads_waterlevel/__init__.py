from homeassistant.core import HomeAssistant
from homeassistant.helpers.discovery import load_platform

DOMAIN = "ads_waterlevel"

def setup(hass: HomeAssistant, config: dict):
    # Plattform-basierte Einrichtung
    return True
